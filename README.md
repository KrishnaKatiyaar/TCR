Reason and applied soultion(after debugging):
When a.py is executed, Python imports c.py and starts running print() statement at the top level and if we put this statement into a function so it will print only when it is explicitly called.
